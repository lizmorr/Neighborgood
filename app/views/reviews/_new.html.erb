<%= form_for [@neighborhood, @review] do |f| %>
  <%= f.label :rating %>
  <%= rating_for @neighborhood, "neighborhood", disable_after_rate: false %>

  <%= f.label :description, "Review" %>
  <%= f.text_area :description %>

  <%= f.submit "Add Review", class: "button" %>
<% end %>
